{% extends 'base.html.twig' %}

{% block title %}Modification du profil : Customer{% endblock %}

{% block body %}
    {% block content %}
        <div class="container">
            <a href="{{ path('account_password') }}" class="btn btn-primary mr-2 mb-2">Modifier mon mot de passe</a>
            <h1>Modification de votre profil</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}

        </div>
    {% endblock %}
{% endblock %}